# Build artifacts
sim/verilator/build/
sw/client/*.o
sw/client/*.a
sw/client/*.so

# Generated operator binaries
sw/operators/*.bin
sw/operators/*.hex

# Eval outputs
eval/results/
eval/figures/

# Synthesis outputs (regenerated by `make synth/impl`)
synth/
*.jou
*.log
.Xil/
# Note: `reports/` is checked in — it holds the canonical
# post-route reports for the historical record.
!reports/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Editor
*.swp
*.swo
.vscode/
.idea/

# OS
.DS_Store

# Vivado run-time droppings (created in CWD by report_*)
clockInfo.txt
cong/
iter_*_CongestedCLBsAndNets.txt
*.pb
*.rpx

# Cloned upstream Corundum (recreated by `make vendor/corundum`)
vendor/corundum/

# Bitstream + intermediate hw build outputs
hw/build/
synth_app/

# Eval results regenerated by `make eval`
eval/results/
eval/figures/
sim/verilator_app/build/
synth_8mp/
*.dcp
reports/u50_2025.2_full_bitstream/fpga_timing_summary_routed.rpt
sim/verilator/build_xdma/

# 322 MHz timing-closure experiment: keep tcl + reports, not checkpoints/logs
synth_322/*.dcp
synth_322/*.jou
synth_322/*.log
synth_322_fp/*.dcp
synth_322_fp/*.jou
synth_322_fp/*.log
synth_322_stdout.log
synth_322_fp_stdout.log

# Fmax sweep: keep tcl + timing reports, not run logs
fmax_*_stdout.log
fmax_*/*.jou
fmax_*/*.log
